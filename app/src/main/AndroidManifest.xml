<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tapjackingdemo">

    <!-- Educational comment: This application demonstrates Tapjacking for educational purposes only. -->
    <!-- Do not use these techniques for malicious activities. -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TapjackingDemo">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Educational comment: This is the main activity of the application. -->
            <!-- It represents the target application that will be obscured by the overlay. -->
        </activity>

        <activity
            android:name=".OverlayActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"
            android:exported="false">
            <!-- Educational comment: This is the overlay activity. -->
            <!-- Its translucent theme is crucial for the Tapjacking attack. -->
            <!-- It's not exported directly as it's launched by MainActivity in this demo. -->
            <!-- IMPORTANT: This app is for educational/testing purposes ONLY. -->
        </activity>

    </application>

</manifest>
